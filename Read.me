📌 実現方法
	•	PC1: TSV → JSON に変換し、JSON ファイルを提供する HTTP サーバー
	•	PC2: PC1 の JSON ファイルを取得し、PC2 に保存するクライアント

⸻

📌 仕組み
	1.	PC1（JSON 変換 & 提供）
	•	受信したリクエストの TSV ファイルを JSON に変換し、HTTP レスポンスとして送信
	2.	PC2（取得 & 保存）
	•	PC1 に HTTP リクエストを送り、JSON を取得
	•	PC2 の指定パスに JSON を保存

⸻

📌 ソースコード

🔹 PC1（JSON 提供サーバー）

PC1 は 受信した TSV ファイルを JSON に変換し、HTTP で提供するサーバー です。

⸻

🔹 PC2（JSON 取得 & 保存）

PC1 の JSON を取得して保存するクライアント です。

